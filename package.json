{
  "name": "indgo-main",
  "private": true,
  "type": "module",
  "scripts": {
    "vercel-build": "npm install --prefix client && npm run build --prefix client"
  },
  "dependencies": {
    "mammoth": "^1.12.0"
  }
}
