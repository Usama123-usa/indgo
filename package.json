{
  "name": "indgo-main",
  "private": true,
  "type": "module",
  "scripts": {
    "vercel-build": "npm install --prefix client && npm run build --prefix client"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "mammoth": "^1.12.0"
  }
}
